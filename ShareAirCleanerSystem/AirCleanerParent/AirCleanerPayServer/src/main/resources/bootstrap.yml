spring:
  application:
    name: pay-server
  profiles:
    active: prod
  cloud:
    config:
      enabled: false
server:
  port: 3002
eureka:
  client:
    service-url:
      defaultZone: http://192.168.13.203:8888/eureka/